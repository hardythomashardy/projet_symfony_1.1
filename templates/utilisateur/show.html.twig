{% extends 'base.html.twig' %}

{% block title %}Utilisateur
{% endblock %}

{% block body %}
   <div class='container-fluid  pt-5 '>
      <div class='row justify-content-md-center '>
         <div class='col-5 '>
            <h1>Utilisateur</h1>
            <div class='pt-3'>
               <table class="shadow table table-bordered table-striped">
                  <tbody>
                     <tr>
                        <th>Id</th>
                        <td>{{ utilisateur.id }}</td>
                     </tr>
                     <tr>
                        <th>Nom</th>
                        <td>{{ utilisateur.nom }}</td>
                     </tr>
                     <tr>
                        <th>Prenom</th>
                        <td>{{ utilisateur.prenom }}</td>
                     </tr>
                     <tr>
                        <th>Age</th>
                        <td>{{ utilisateur.age }}</td>
                     </tr>
                  </tbody>
               </table>
            </div>
            <div class='row ml-2'>

               <a class='shadow btn btn-primary mx-2' href="{{ path('utilisateur_index') }}">back to list</a>
               <a class='shadow btn btn-success ' href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}">edit</a>
               <div class='mx-2'>
                  {{ include('utilisateur/_delete_form.html.twig') }}</div>
            </div>
         </div>

      </div>
   </div>
{% endblock %}
